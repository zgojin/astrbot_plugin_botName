{
  "bot_name": {
    "type": "string",
    "description": "机器人名字 (将作为群名片的开头)",
    "hint": "如果留空，则不显示名字。",
    "default": "插件已加载，插件配置面板可修改格式"
  },
  "cpu_prefix": {
    "type": "string",
    "description": "CPU部分的前缀",
    "hint": "显示在CPU数值之前的内容。例如：🧠、cpu占用:",
    "default": ""
  },
  "cpu_suffix": {
    "type": "string",
    "description": "CPU部分的后缀",
    "hint": "显示在CPU数值之后的内容。例如：%",
    "default": "%"
  },
  "memory_prefix": {
    "type": "string",
    "description": "内存部分的前缀",
    "hint": "显示在内存数值之前的内容。例如：💾、内存占用:",
    "default": ""
  },
  "memory_suffix": {
    "type": "string",
    "description": "内存部分的后缀",
    "hint": "显示在内存数值之后的内容。例如：%",
    "default": "%"
  },
  "time_prefix": {
    "type": "string",
    "description": "时间部分的前缀",
    "hint": "显示在时间数值之前的内容。例如：🕒、时间",
    "default": ""
  },
  "time_suffix": {
    "type": "string",
    "description": "时间部分的后缀",
    "hint": "显示在时间数值之后的内容。",
    "default": ""
  },
  "separator": {
    "type": "string",
    "description": "各个指标之间的分隔符",
    "hint": "例如：' | ' 或 '  ,  '",
    "default": ","
  }
}
